{
  "name": "vr-proje-vercel-gateway",
  "version": "1.0.0",
  "private": true,
  "description": "Vercel serverless AI gateway for the VR-PROJE Modul3 doctor chat. The Unity project itself is not built from this package.json.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0"
  },
  "devDependencies": {
    "@vercel/node": "^3.2.0",
    "typescript": "^5.4.5"
  }
}
